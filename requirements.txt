# DocRetriever -- Lightweight deps for the Streamlit dashboard (cloud deploy)
# For the full stack (API, pgvector, embeddings, reranker) use: pip install -r requirements-full.txt
streamlit==1.39.0
pandas==2.2.3
matplotlib==3.9.2
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
